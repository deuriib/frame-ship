# Test / Evidence Matrix: SPEC-single-dispatcher-engineering

**Agent:** engineering implementer (single-track direct execution per execute-spec SKILL §3.3 — the dispatched ONE; no sub-dispatch)
**Date:** 2026-09-16
**Domains-Touched:** [engineering]
**REQ-007:** CANCELLED (CEO decision #3 — supersedes CEO decision #2; nothing created/edited under `skills/templates/implementers/`)

| REQ-ID | Evidence ID | Description | Type | Status | Artifact |
|--------|-------------|-------------|------|--------|----------|
| REQ-001 | E-001 | Plugin dispatch contract (working-tree diff `git diff .opencode/plugins/frame-ship.ts`): WORKFLOW_CARD modes line (`:19`) states montilla (CEO) is the sole dispatcher — `task(general)` max2, **CEO dispatches entire team; c-levels/specialists do the work or brief back — cross-domain need → formal Cross-domain request brief to CEO, who delegates to the right agent or resolves**; MONTILLA_OWNERSHIP (`:34`) "sole dispatcher to the entire team" + brief-back + "you delegate to the right agent (task(general) max 2 parallel) or resolve". Any `task(` mention scopes caller to montilla. `hasMarker()` (`:36-39`) + `import type` zero-dep unchanged | Review + Attestation | pass | `git diff` + file read `:19,:34,:36-39` (AC-001) |
| REQ-002 | E-002 | Version v0.3.3 identical in all 4 locations: header comment `:2`, `VERSION` `:10`, `MARKER` `:11` (derived), `.opencode/plugins/AGENTS.md:4` (drift v0.2.0 → v0.3.3 fixed by same bump; v0.3.1/v0.3.2 already used in CHANGELOG) | Attestation | pass | grep 4 locations + file reads + `git diff` (AC-002, C-3) |
| REQ-003 | E-003 | Uniform sentence **"CEO dispatches entire team; c-levels/specialists do the work or brief back"** verbatim (santana contract) in all 7 locations: `using-frame-ship/SKILL.md:64`; `tool-mapping.md:8,17,35`; `bootstrap-checklist.md:11`; `translate-to-spec/SKILL.md:29,32`; `propose-changes/SKILL.md:25,30`; `execute-spec/SKILL.md:23,33`; `quality-gate/AGENTS.md:4` ("Owns the fan-out" → "Only stage whose reviewers the CEO dispatches in parallel"). Old verbs grep ("never dispatches", "never fan out", "NEVER fans out", "no C-level fan-out") = 0 outside allowed mechanism descriptions | Review | pass | `git diff` 7 files + rg counts (AC-003) |
| REQ-003 | E-004 | 13 verify-only mechanism lines **unchanged vs HEAD** (files absent from `git status` M list) + CEO-named (read-through): `frame-intent:34` (montilla-owned stage, no non-CEO caller), `review-security:28` ("barrera runs inside the CEO's `task(general)`"), `review-architecture:28` ("CEO dispatches `task(general)`"), `quality-gate:28` ("the CEO dispatches all gate reviewers"), `:30` ("CEO-dispatched `task(subagent_type=general)`"), `:56` ("Each reviewer dispatched by the CEO"), `:58` ("CEO `task(...)` max 2 parallel"), `verify-handoff:27` + `ship-release:30` ("CEO dispatches `task(subagent_type=general)`") | Review | pass | `git status` + line reads (AC-003 verify-only) |
| REQ-004 | E-005 | tool-mapping §"CEO-only dispatch rule" (`:6-8`) documents the formal brief-back flow: c-level/specialist with cross-domain need returns it **inside the agent's return** as a formal **Cross-domain request** brief (Need + Reason + Suggested owner + Urgency) → CEO delegates to the right agent (`task(general)` max 2 parallel) **or resolves**; generic-harness fallback = single mode. No flag-don't-grab residue; prompt-read order + reference-only packets intact | Review | pass | tool-mapping.md diff + read-through (AC-004) |
| REQ-005 | E-006 | AGENTS.md ×3 carry the uniform sentence + brief-back line — grep 3/3: root `AGENTS.md:37` (new **Dispatch** clause in CONVENTIONS — closes the POINTERS gap `frame-ship.ts:30`), `skills/AGENTS.md:26` (Role owners line extended: sole dispatcher to entire team + ADR-003 sentence), `.opencode/plugins/AGENTS.md:19` (dispatch contract note) | Attestation | pass | 3 file diffs + grep (AC-005, KR-1.2) |
| REQ-006 | E-007 | ADR-003 **already filed** by propose-changes stage — verify-only, no rewrite (C-4): `docs/specs/10_design/ADR-003-ceo-only-dispatch.md` exists (2026-09-16, status accepted) with Decision §1-§6: §4 scope incl. implementer-prompts location, §5 harness adapters (opencode `task`/Codex provisional/generic fallback), §6 AC-001 grep scope + exemptions; prior citations (`TEST_MATRIX-ceo-only-dispatch.md:9,27,30,41`, `IMPLEMENTATION_PLAN-ceo-only-dispatch.md:5,30`, `skills/AGENTS.md:24`) resolve | Review | pass | ADR-003 file read: `:23-61` (AC-006) |
| REQ-007 | E-008 | **CANCELLED (CEO decision #3).** No create/edit under `skills/templates/implementers/`. Existence check recorded (C-2): 4 files on disk (`engineering-implementer.md`, `engineering-reviewer.md`, `engineering-qa-verifier.md`, `generic-implementer.md`) — untracked leftovers from a prior run (committed-deleted at `fdfca0a..7fe71c8`), left untouched; keep-or-remove is a CEO commit decision | Attestation | cancelled | glob + `git status` + `git log` (AC-007 N/A; AC-REQ-F-008 voided for engineering track) |
| REQ-NF-001 | E-009 | PII/secrets scan (Ley 172-13) over the changed-file set (plugin, 3 AGENTS.md, 7 skill files, ADR): patterns `api_key|secret|token|password|credential|private key|AKIA|JWT|bearer|BEGIN PRIVATE KEY` = **2 hits, both prohibition clauses** (`frame-ship.ts:25,26` guardrail strings); zero PII/credentials/tokens; finding-without-proof = REFUTED, scan output is the evidence | Review | pass | rg scan output (AC-008, barrera C-2) |
| REQ-NF-002 | E-010 | Zero-residue grep over engineering layer (`skills/`, `.opencode/plugins/frame-ship.ts`, 3 AGENTS.md files) for "flag it in your return", "suggested owner", "fan out", "route onward", "never dispatches", "never fan out" (+ AC-003 variants "NEVER fans out", "no C-level fan-out", "C-level returns, never dispatches", "flag don't grab", "routes work onward"): **1 hit total** — `tool-mapping.md:8` (NEW mechanism description of the CEO-only rule; explicitly exempt as mechanism documentation per ADR-003 §Decision.6 + REQ-NF-002). 0 in plugin, 0 in AGENTS.md ×3, 0 in other skills. `agents/` legacy wording = santana people track (68 templates), not engineering scope | Attestation | pass | rg counts per layer, hits with context (AC-009, barrera C-4) |
| REQ-NF-003 | E-011 | Rollback table in `IMPLEMENTATION_PLAN-single-dispatcher.md` §Rollback Points: one revert point per layer (plugin/git revert + tsc re-run; skills per-file revert; AGENTS.md ×3; ADR verify-only no-delete; prompts = cancelled/no-created + untracked leftovers note; evidence docs delete) + ETA < 15 min; rollback note rides HANDOFF (verify-handoff stage) | Attestation | pass | plan §Rollback Points (AC-010) |
| REQ-NF-004 | E-012 | `npx -y -p typescript tsc --noEmit --skipLibCheck --module nodenext --target es2022 --moduleResolution nodenext plugins/frame-ship.ts` run from `.opencode/` → **exit 0**; plugin remains single-file zero-dep | Test | pass | tsc output (AC-002, barrera C-3) |

Types: `Unit | Integration | E2E | Review | Sign-off | Attestation | Launch-check | Filing-proof`. Non-code REQs use review/sign-off/attestation with artifact path — REQ-ID trace mandatory for all 8 domains.

## Condition Trail (GATE: none-yet — conditions binding on execute-spec, evidenced above)

| Condition | Source | Evidence |
|-----------|--------|----------|
| C-1 verbatim contract (no paraphrase) | ARCHITECTURE_REVIEW `:47-50` / SECURITY_REVIEW C-1 | E-003/E-005: uniform sentence + W8 line copied verbatim from SPEC-single-dispatcher-people §4 W2/W8; gate diff = 0 bytes target |
| C-2 prompts existence check + no silent creation | ARCHITECTURE_REVIEW `:51-55` / SECURITY_REVIEW C-1 | E-008: existence recorded (4 untracked files), CANCELLED per CEO decision #3, untouched |
| C-3 version bump v0.3.3 4 locations identical | ARCHITECTURE_REVIEW `:56-59` | E-002: header/VERSION/MARKER/plugin-doc all v0.3.3 |
| C-4 ADR-003 content (not only existence) | ARCHITECTURE_REVIEW `:60-63` | E-007: Decision §4/§5/§6 verified by read |
| C-5 gate attestations (barrera wording-only, people gate) | ARCHITECTURE_REVIEW `:64-67` / SECURITY_REVIEW C-5 | E-009/E-010: scan prohibition-only + residue 0; full review wave re-verifies at quality-gate |

## REQ-007 Cancellation Note (CEO decision #3)

- Approved change list for this unit excludes implementer prompts; **no file under `skills/templates/implementers/` was created or edited** by this run.
- Pre-existing untracked leftovers (`?? skills/templates/` per `git status`) were verified present (matching the 4 approved names) and left untouched — they were committed-deleted at `fdfca0a..7fe71c8`; committing them again or removing them is the CEO's call.
- Stale references to the prompts location now exist outside the changed lines (`tool-mapping.md:13` portable-implementer-prompt mention; `skills/AGENTS.md:24` scoped-location sentence; `plugins/AGENTS.md` WHERE-TO-LOOK). Not in the approved change list (REQ-003 itemized lines only) — flagged to CEO as follow-up: decide keep/purge prompts + sync references.

## Coverage Summary

- Unit coverage: N/A — docs/config-only change (non-code; every REQ evidenced by review/attestation/scan/test instead of code tests)
- Integration coverage: N/A — docs/config-only
- Evidence coverage: 11/11 REQ-IDs (REQ-001..006 + NF-001..004 pass; REQ-007 cancelled with existence-check evidence E-008)
- Acceptance criteria covered: 9/10 (AC-001..006 pass, AC-008..010 pass; AC-007 N/A — REQ-007 cancelled by CEO decision #3)