# Readability Review: SPEC-repo-hygiene

**Reviewer:** review-readability (independent — readability lens only; no reliability/resilience/risk/QA/automation verdicts bundled)
**Date:** 2026-09-23
**Verdict:** pass

Packet accepted by reference: `SPEC:docs/specs/20_backlog/SPEC-repo-hygiene.md#REQ-001..007+REQ-NF-001..003 / HARD:subagents+zero-dep+no-runtime-behavior-change(sole exception: sponsor-approved REQ-007)+sequential-lane / GATE:none-yet → this verdict / DOMAINS:[engineering,automation/ops]`.

Lens surfaces reviewed: `README.md`, root `AGENTS.md`, `plugins/AGENTS.md`, chain artifacts (`SPEC-repo-hygiene.md`, `PROPOSED_CHANGES.md`, `IMPLEMENTATION_PLAN.md`, `TEST_MATRIX.md`, REQ index), hook header comments, `tests/smoke.test.mjs`, `.github/workflows/ci.yml`, CHANGELOG sections touched by this work. The prior reviewer artifact `refuter.md` was read as context only — every proof below was re-executed independently by this reviewer; git stayed read-only; one file written: this verdict.

## Checklist

- [x] Naming is intention-revealing — no `data`/`tmp`/`x` names. Tests use `ROOT`/`SKILLS_TS`/`HOOKS_DIR`/`runHook`/`extractHookBodies`; CI jobs are named by duty (`typecheck`, `test`); test blocks `(a)/(b)/(c)` map 1:1 to REQ-002 clauses.
- [x] Functions have single responsibility — each test targets one assertion class; `parseFrontmatter`/`skillDirsFromSource`/`extractHookBodies`/`runHook` do one thing each.
- [x] Nesting depth <= 3 — deepest path is `extractHookBodies` (while → if/else = depth 3); hook transform blocks are shallow if-chains.
- [~] Comments explain WHY, not WHAT — hook headers document I/O contracts plus security rationale ("reason names the RULE, never echoes the matched value"); test comments justify contract choices ("exit 0 always — the block travels as decision deny"). **Exception:** one stale design comment in `agents.ts` claims disabled behavior as active → RD-004.
- [x] Public APIs documented — all three hook headers state stdin/stdout/exit contracts; every documented command was executed by this reviewer and runs (see Verdict Rationale).
- [~] No dead code or commented-out blocks — the commented `PLAN_DESCRIPTION`/`BUILD_DESCRIPTION` lines are the spec-mandated design (REQ-007: overrides stay commented), not dead code; but the block comment above them describes them as active → RD-004.
- [x] Consistent style with surrounding code — test file matches `plugins/opencode` style (2-space, double quotes, JSDoc headers); `ci.yml` follows conventional Actions shape; merges preserved Keep-a-Changelog structure byte-for-byte.

## Findings

| ID | Severity | Finding | Proof |
|----|----------|---------|-------|
| RD-001 | Medium | **README still describes a two-plugin runtime — structure that misleads.** `README.md:162` "Split zero-deps V2 runtime (**two plugins** + shared module + composed entry)"; `:166` and structure comment `:237` "runs **both lanes**"; `:283` "**all four** `plugins/opencode/*.ts` files"; the Plugin Runtime list (`:163-166`) and the structure block (`:235-240`) omit `guardrails.ts` entirely — while the same README compiles it (`:262`, `:266`, byte-identical to `mise.toml`, 251/251 bytes) and credits the guardrails lane at `:95`. The directory holds **5** `.ts` files. Contradicts `plugins/opencode/frame-ship.ts:1-5,20` (imports skills + agents + **guardrails** lanes), `plugins/AGENTS.md:26` ("three independent plugins; `frame-ship.ts` composes all three"), root `AGENTS.md:37` ("runs all three lanes"), and `skills.ts:7` ("guardrails live in `./guardrails.ts`") vs README `:163` (attributes guardrails injection to `skills.ts`). A competent reader cannot tell which doc is true; this is the exact drift class this spec exists to close ("structure that misleads", spec §1). Not among REQ-004's enumerated targets — pre-existing, but in the flagship doc the lane edited twice (`ae97e42`, `98d8182`). **Owner:** engineering owner | `grep -n "both lanes\|two plugins\|all four" README.md` → 162,166,237,283; `ls plugins/opencode/` → `agents.ts frame-ship.ts guardrails.ts skills.ts shared.ts`; structure block lines 235-240 list only INSTALL/frame-ship/skills/agents/shared; `grep -n guardrails README.md` → 39,95,162,163,262,266,283 (none inside the structure block) |
| RD-002 | Low | **Test file header instructs a command that fails on the repo's pinned Node 22.** `tests/smoke.test.mjs:12` — `Run: node --test tests/`. Re-executed by this reviewer: under pinned `mise exec node@22` (v22.23.2, same as `mise.toml`, README badge, and CI `node-version: 22`) → **exit 1**, `Error: Cannot find module '.../frame-ship/tests'`, `tests 1 / pass 0 / fail 1`; under default node v26.9.0 → 9/9 exit 0. This is precisely failure F-006/deviation 1, which spec `:19`, proposal `:22`, and matrix `:29` document as remediated ("spec + proposal now state the glob form") — the test file's own header was never in the sweep and still carries the broken form. A reader following the file on the canonical runtime sees a module-not-found failure and cannot tell tests are actually green via `npm test`. **Owner:** engineering owner | `mise exec node@22 -- node --test tests/` → exit 1, TAP head: `Error: Cannot find module '/mnt/DATA/GitHub/frame-ship/tests'` … `# tests 1 / # pass 0 / # fail 1`; default `node --test tests/` → `ℹ tests 9 ℹ pass 9`, exit 0; `npm test` → 9/9 exit 0; remediation scope quote `TEST_MATRIX.md:29` ("spec + proposal now state the glob form") |
| RD-003 | Low | **Proposal's packet line drops the NF segment — reference-only envelope inconsistency.** `PROPOSED_CHANGES.md:8` Packets SPEC anchor reads `#REQ-001..007` without `+REQ-NF-001..003`, while the canonical sender `SPEC-repo-hygiene.md:46`, the plan `IMPLEMENTATION_PLAN.md:7`, and the proposal's own Spec Reference `PROPOSED_CHANGES.md:3` all carry `#REQ-001..007+REQ-NF-001..003`. Same class as cleared CE-004 (packet anchors) — that sweep normalized the REQ range but missed the NF tail on line 8. Effect: the receiver envelope reads as if zero-dep/reversibility/traceability were out of scope at that hop. **Owner:** engineering owner | `PROPOSED_CHANGES.md:8` → `SPEC:…#REQ-001..007 / HARD:…` (no NF); `PROPOSED_CHANGES.md:3` → `…#REQ-001..007+REQ-NF-001..003`; `SPEC-repo-hygiene.md:46` and `IMPLEMENTATION_PLAN.md:7` → `…+REQ-NF-001..003` |
| RD-004 | Low | **`agents.ts` block comment claims description overrides that are disabled by design.** `plugins/opencode/agents.ts:481-483` states "plan gets the Frame-lane **description**, … build gets the Ship-lane **description**", but the implementing lines `:490` `// agent.description = PLAN_DESCRIPTION;` and `:500` `// agent.description = BUILD_DESCRIPTION;` are commented out per REQ-007 ("built-in descriptions untouched"; commit body `1640510` honestly records "stay commented by design"). The inline comment describes the pre-disable state, so a maintainer reading the WHY block is told behavior the shipped code does not perform (risk: "cleanup" by uncommenting = unapproved behavior change). Comment-quality/wording drift only — the adjacent code lines show the truth. **Owner:** engineering owner (sponsor-authored block — sponsor confirms wording) | `sed -n '470,500p' plugins/opencode/agents.ts` → comment lines 481-483 claim descriptions; lines 490/500 commented; `git show 1640510` → description lines re-indented but kept commented (`-    //       agent.description` → `+          // agent.description`); spec `SPEC-repo-hygiene.md:23` "overrides stay commented (built-in descriptions untouched)" |

Findings = 4 (1 Medium, 3 Low). No finding has freelance-fixed anything: severity + location + owner reported only.

## Verdict Rationale

- **conditional** = real findings exist with available, cheap mitigations; none invalidates the spec's core, but known wording defects should clear before ship. `fail` weighed and rejected: every reader-critical surface re-verified green — `npm test` 9/9 (exit 0), `mise run typecheck` exit 0, `npm run version:check` all 7 files synchronized at v0.11.0, README replay commands (lines 103-104) and `plugins/AGENTS.md` replay commands all execute with correct output (`decision:allow`, bootstrap marker `[frame-ship v0.11.0]`, `{}`), raw `tsc` line byte-identical to `mise.toml` (251 bytes), CHANGELOG heading counts `v0.6.1`/`v0.7.0`/`v0.8.0`/`v0.3.0`/`v0.3.1` = 1/1/1/1/1 with zero duplicated `### Added` per section (awk scan), lane CHANGELOG delta purely structural (+5/−14: header insert + two verbatim merges), every implementation commit body carrying a `REQ-00N → AC-00N` trace line, and root `AGENTS.md` skill arithmetic now holding (12 = 1 + 9 + 2). The chain artifacts themselves are high-clarity: proposal table/rationale/alternatives/rollback complete, plan steps map 1:1 to single-revert commits with an explicit order-of-operations, matrix rows carry per-REQ status + deviation records.
- `pass` weighed and rejected: RD-001 leaves the flagship doc contradicting three canonical sources inside this review's own lens — shipping a doc-drift spec with same-class drift still open in README would be an unsilent residual — and RD-002 ships a run-command comment that fails on the pinned runtime (the very failure the F-006 remediation exists for). A readability reviewer cannot pass what it just reproduced red.
- Conditions to clear (gate keeper maps to COND-00x), all one text-only/comment-only sweep through the established gate-finding remediation path (text-class amendment first, per proposal-before-code precedent `a4c1c8f`): **COND-001** = RD-001 README runtime/structure sweep (`two plugins`→three, `both lanes`→all three lanes or enumerate, add `guardrails.ts` to structure block + Plugin Runtime list, fix the `all four` count, align the `skills.ts` guardrails attribution); **COND-002** = RD-002 test header `Run:` line → `npm test` (or the shipped quoted-glob form); **COND-003** = RD-003 restore `+REQ-NF-001..003` on `PROPOSED_CHANGES.md:8`; **COND-004** = RD-004 reword the `agents.ts` block comment to state descriptions stay built-in (comment-only, sponsor-confirmed).
- Residual risk stated, not assumed: this review adjudicates wording/readability only — correctness of the fixes, CI mechanics, and evidence claims belong to the sibling reviewers; no first-push CI run, no dependency-graph, and no behavior claim was inherited or re-issued here. Zero PII/secrets/tokens in this artifact; exactly one file written: this verdict.

## Re-verification (post-remediation, 2026-09-23)

Scope: condition-clearance of THIS reviewer's COND-001..004 (findings RD-001..RD-004) only — not a new full review, no other reviewer's conditions touched (independence preserved). Every proof below was re-executed fresh by this reviewer after the wave-2 commits; git stayed read-only; one file written: this verdict. The original findings table above is unmodified. Wave-2 commits observed in `git log` but NOT adjudicated here: `f14cc4c` (proposal/plan amendments), `4526b63` (RD-001/RD-002 fixes + RL-001, another reviewer's condition), `2de0839` (RL-002/AUT-003/risk-3 lockfile), `fdda81a` (RD-004), `449f386` (matrix rows for QA/AUT/risk findings).

| RD | Status | Decisive evidence (re-run 2026-09-23) |
|----|--------|----------------------------------------|
| RD-001 | **STILL-OPEN — 4 of 5 COND-001 clauses CLEARED, clause 5 unexecuted** | **Cleared (fresh reads of `4526b63` state):** (a) `README.md:162` now "three plugins + shared module + composed entry"; (b) `README.md:167` "runs **all three lanes**" + structure comment `README.md:238` "all three lanes" — `grep -n "both lanes\|two plugins\|all four"` → **0 hits**; (c) `guardrails.ts` present in both surfaces: Plugin Runtime bullet `README.md:165` "**`guardrails.ts`** (id `frame-ship-guardrails`) — injects the full `rules/guardrails.md` on context + the minimal one-liner set on compaction" and structure block `README.md:241`; (d) anti-pattern `README.md:285` "all **five** `plugins/opencode/*.ts` files"; cross-checks hold — bullet wording matches `plugins/AGENTS.md:11` (same id, same full/minimal split) and `frame-ship.ts:1-5,20` (imports all three lanes). **Still open (clause 5, "align the `skills.ts` guardrails attribution"):** `README.md:163` still reads "skills.ts … injects workflow card **+ guardrails +** pointers via `ctx.session.hook(\"context\")`", contradicting three freshly-read sources that assign guardrails injection to its own lane: `plugins/opencode/skills.ts:7` ("guardrails live in `./guardrails.ts`" — `grep -n guardrails plugins/opencode/skills.ts` returns only the header parenthetical and the POINTERS truth-string, no guardrails content injection), `plugins/AGENTS.md:11` (guardrails.ts owns the full/minimal injection), and README's own new bullet at `:165` two lines below (double attribution within one list). `PROPOSED_CHANGES.md:83`/`IMPLEMENTATION_PLAN.md:23` claim the wave covered "README three-plugin + guardrails truth" — they do not claim the skills.ts line was aligned, so no claim is falsified; the COND-001 clause simply was not executed. **Owner:** engineering owner — one text line |
| RD-002 | **CLEARED** | `tests/smoke.test.mjs:12` now: `Run: npm test   (= node --test "tests/*.test.mjs"; the bare `tests/` dir form exits 1 on node 22)` — documents the working form **and** the pinned-runtime failure note (the exact F-006 semantics). Fresh `npm test` → **exit 0** on the post-remediation tree (9/9 previously re-run this session on the same test body; header change cannot alter execution) |
| RD-003 | **CLEARED** | All four packet locations freshly re-read, now uniform: `PROPOSED_CHANGES.md:8` → `…#REQ-001..007+REQ-NF-001..003 / HARD:…`; matches canonical sender `SPEC-repo-hygiene.md:46`, `IMPLEMENTATION_PLAN.md:7`, and the proposal's own line 3. `grep -n "REQ-001..007"` across the three files → NF tail present at proposal:3, proposal:8, spec:46, plan:7; sole remaining `REQ-001..006`-style literal = `PROPOSED_CHANGES.md:82` before→after remediation prose (audit-trail quotation, already classified as such in refuter CE-004 clearance — not an envelope anchor). Fix commit `f14cc4c` lands text before its fix-class records per proposal-before-code |
| RD-004 | **CLEARED** | `git show fdda81a --stat` → `plugins/opencode/agents.ts` only, `+9/−4`; line-level diff inspection: **every** `+`/`−` line is a `//` comment — zero logic change proven from the diff itself, not just the stat. Rewritten block (`agents.ts` ~L479-489) now reads "**PLAN_DESCRIPTION/BUILD_DESCRIPTION overrides stay commented by REQ-007 design — built-in descriptions untouched**" plus the permission-persistence note ("host-reconciled … must be confirmed in a live host session — gate-tracked residual, engineering owner, expiry at first use"); the stale "plan gets the Frame-lane description / build gets the Ship-lane description" claims are gone; the `// agent.description = …` lines remain commented (REQ-007 invariant intact). Commit body records "(RD-004, risk-2)" and is sponsor-authored (commit author = sponsor = the directive source → sponsor-confirmed by authorship). Fresh `mise run typecheck` → **exit 0**; fresh `npm test` → **exit 0** on this tree |

**Verdict after re-verification: conditional (unchanged).** Reasoning, stated explicitly:

- 3 of 4 keyed findings (RD-002, RD-003, RD-004) cleared fully with fresh proof; RD-001 did **not** genuinely clear — its COND-001 text enumerated five clauses and one was not executed (proof above). Per this pass's contract, `pass` requires all four to clear; any that genuinely does not keeps `conditional`.
- On whether a Low blocks: the RD-001 residue **on its own severity today** is Low (the misleading structure/counts/lane claims — the Medium core — are all fixed; only a redundant stale attribution next to the now-correct bullet remains), and per guardrails Low = backlog = non-blocking for findings recorded *elsewhere* in this file. It nonetheless holds the verdict here for two reasons: (1) it is an unexecuted clause of COND-001 on one of the four keyed findings, not an "elsewhere" backlog item — conditions clear or the verdict stays; (2) gate precedent in this same wave: refuter held `conditional` on two Low-severity conditions (CE-003/CE-004) until they were executed, then moved to `pass` — consistency of condition mechanics matters more than the severity of the last mile.
- Remaining work to reach `pass`: **one line** — reword `README.md:163` so `skills.ts` no longer claims guardrails injection (align with `skills.ts:7`'s own wording). No freelance fix performed here; owner executes, this reviewer re-reads, verdict flips.
- Post-remediation regression checks all green: `npm test` exit 0, `mise run typecheck` exit 0, `git status --porcelain` shows only this `quality-gate/` directory (untracked scratch, expected mid-gate). Zero PII/secrets in this artifact; exactly one file written across both passes: this verdict.

### Clause-5 closure (RD-001 → CLEARED, 2026-09-23, commit `11d7ff6`)

Scope: the single open item from the pass above — COND-001 clause 5 ("align the `skills.ts` guardrails attribution"). Every proof re-executed fresh by this reviewer after `11d7ff6`; git stayed read-only; one file written: this verdict. The pass-1 table and its `conditional` paragraph above remain unmodified as the historical record of that pass.

| Clause | Status | Decisive evidence (re-run 2026-09-23) |
|--------|--------|----------------------------------------|
| RD-001 clause 5 | **CLEARED** | `git show 11d7ff6 --stat` → `README.md` only, `+1/−1` — exactly the one line, nothing else touched. Diff (line-level): `- … injects workflow card + guardrails + pointers via \`ctx.session.hook("context")\`, preserves chain…` → `+ … injects workflow card + pointers + live bootstrap via \`ctx.session.hook("context")\` (guardrails injection lives in \`guardrails.ts\`), preserves chain across compaction…`. Fresh `sed -n '163p' README.md` shows the new text in place; fresh `sed -n '1,12p' plugins/opencode/skills.ts` header reads "injects workflow card + pointers + live bootstrap (guardrails live in `./guardrails.ts`), and preserves the chain across compaction" — README bullet now matches the source header claim-for-claim (same three injections, same compaction clause, guardrails attribution deferred to its own lane). Repo-wide falsification attempts all negative: `grep -c "workflow card + guardrails" README.md` → **0** (false claim gone from the entire README); `grep -c "workflow card + pointers" README.md` → 1; `grep -nE "both lanes|two plugins\|all four" README.md` → **0 hits** (exit 1). All remaining README `guardrails` hits classified legitimate on re-read: `:39` prose, `:95` agy mapping row (credits the guardrails lane), `:163` the new correct attribution, `:165` bullet, `:241` structure, `:264`/`:268` compile lists. Regression on this tree: fresh `npm test` → **exit 0**; `git status --porcelain` → only this untracked `quality-gate/` scratch dir. Commit `11d7ff6` body records "(RD-001 c5)" + cites the COND-001 clause and source-header truth, sponsor-authored (author = sponsor = owner directive source) |

**COND-001 status: 5/5 clauses CLEARED** — (a) `README.md:162` three plugins, (b) `:167`/`:238` all three lanes, (c) `:165`+`:241` `guardrails.ts` in both surfaces, (d) `:285` all five `.ts` files (all verified in the pass above), (e) `:163` attribution aligned to source (this pass). RD-002, RD-003, RD-004 cleared in the pass above — **all four keyed findings, and every clause of their conditions, are now closed with fresh proof.**

**Final verdict: pass** — supersedes the `conditional` recorded in the pass-1 block above (that block stays as unmodified historical record, per re-verification convention). Nothing genuinely remains open in this reviewer's scope; residuals noted elsewhere (first-CI-run checkpoint, live-host permission confirmation, lockfile registry provenance) belong to other reviewers' lenses or ship-release checkpoints and were not adjudicated here. Zero PII/secrets/tokens in this artifact; exactly one file written across all three passes: this verdict.
