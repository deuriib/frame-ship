# Risk Review: SPEC-repo-hygiene

**Reviewer:** review-risk (risk lens only — no other reviewer's role performed)
**Date:** 2026-09-23
**Verdict:** pass  *(post-remediation re-verification, 2026-09-23 — pass-1 verdict was **conditional**; the checklist, findings table, and rationale below are preserved as the pre-remediation record and are superseded by ## Re-verification at the end)*

**Packet (accepted by reference):** `SPEC:docs/specs/20_backlog/SPEC-repo-hygiene.md#REQ-001..007+REQ-NF-001..003 / HARD:subagents+zero-dep+no-runtime-behavior-change(sole exception: sponsor-approved REQ-007)+sequential-lane / GATE:none-yet→pass|conditional|fail produced here / DOMAINS:[engineering,automation/ops]`

Prior artifact `refuter.md` (pass post-remediation) read as context only; every proof below was re-executed independently. Git stayed read-only; exactly one file written: this verdict. Zero PII/secrets.

## Checklist

- [x] **Blast radius bounded and verified** — `git diff --name-only origin/main..HEAD` = exactly the 18 approved files (workflow, tests, lockfile+script, docs/changelog, hook headers, skills.ts comment, agents.ts REQ-007). NOT in range: `docs/specs/50_archive/`, `10_design/ARCHITECTURE.md`, `12_adr/`, `rules/`, `agents/*.md`, skill bodies. Hook `.ts` diffs verified comment/shebang only (+2/−2 each); `skills.ts` = one comment line (13→12). History 19 ahead / 0 behind origin — nothing public.
- [x] **Backward compatibility preserved** — no public API/schema/data-model shift → no ADR required (spec §4 rule: CI + tests are repo tooling, not `ARCHITECTURE.md` components). Sole runtime delta = sponsor-approved REQ-007.
- [x] **Dependencies pinned; zero critical/high CVEs or supply-chain hazards** — `package.json` range diff = script line only (`+2 −1`), `dependencies` untouched (REQ-NF-001). Mutable major-tag actions = R-006 **documented accepted risk** (owner: engineering owner; expiry: public flip → SHA pins + Dependabot — spec §4 + proposal R-006), satisfying the guardrail for accepted risks. Lockfile mirror URLs integrity-pinned (RK-003 Low); unpinned `npx` TypeScript (RK-004 Low). No Critical/High supply-chain hazard.
- [x] **Rollback determinism proven (≤15 minutes)** — repo-only tooling/docs, unpushed, no data/external comms; per-unit commits atomic; `git show <sha> | git apply --check -R` clean for 7/10 tested units, 3 units conflict-as-worded but resolve in minutes via newest-first revert or file delete (RK-005 Low = attestation wording, not capability). CI disable = delete/revert one file; tag removal = `git tag -d`.
- [x] **Architectural contract invariants intact** — `ARCHITECTURE.md` absent from the range diff; INV-007 (historical immutability) holds: zero edits under `50_archive/`, prior briefs, archived ADRs.
- [ ] **Zero unmitigated regression risk across touched systems** — NOT met: RK-002 (the sole behavior change's permission enforcement is unproven and internally contradicted) and RK-001 (false provenance claim inside the REQ-NF-003 evidence chain) are open at gate time.

## Findings

| ID | Severity | Finding | Proof |
|----|----------|---------|-------|
| RK-001 | Medium | **False provenance claim in the gate evidence chain.** `TEST_MATRIX.md:25` (orchestrator verification block) states lockfile `registry.npmmirror.com` URLs are "pre-existing since `7efa7f7`" — they were **introduced by this spec's own remediation commit `32210cf`** (CE-002 lockfile optionals, executed on a machine with a mirror-configured npm). The claim is inherited verbatim at `refuter.md:71` ("pre-existing since `7efa7f7` … not adjudicated by this pass"). Impact: a newly-introduced supply-chain surface is framed to the gate as legacy, shaping CE-002's aftermath unchallenged; REQ-NF-003's traceability chain carries a falsified verification line in its highest-trust section. **Owner:** orchestrator (author of the verification block; refuter re-records its own residual note). | `git show 7efa7f7:package-lock.json \| grep -c '"resolved"'` → **0**; `grep -c 'registry.npmmirror.com'` → **0**; `git log --oneline -S 'registry.npmmirror.com' -- package-lock.json` → **only `32210cf`**; current file: 5 npmmirror `resolved` URLs (package-lock.json:527,540,553,566,590); claim text read at TEST_MATRIX.md:25 + refuter.md:71 (2026-09-23). |
| RK-002 | Medium | **REQ-007 permission enforcement is unverified at runtime and internally contradicted.** The transform assigns `agent.permissions = planPermissions()` to the built-in `plan` agent (`plugins/opencode/agents.ts:489`), while the file's own contract comments state "**runtime `editor.update()` mutations for permissions are overwritten by the host, frontmatter sticks**" (`agents.ts:233-237`, restated `:413-415`) — and built-in plan/build carry no frame-ship frontmatter. Official V2 docs say the opposite for transforms ("the next read rebuilds it by replaying every active transform in registration order onto a fresh value" — opencode.ai/v2/docs/build/plugins, fetched 2026-09-23), so the direction is genuinely unresolved: docs → enforcement holds; the repo's own comment → the docs-only/shell-deny sandbox is a **silent no-op**. AC-008's evidence (diff + typecheck + `npm test`) is static w.r.t.t. this behavior — the 9-test smoke suite never imports or executes `agents.ts`. Conditional impact on the single HARD-exempted behavior change. Presence/idempotency sub-questions are SAFE (see RK matrix note). **Owner:** engineering owner (sponsor-directed fold-in; spec owner adjudicates). | `agents.ts:489` assignment vs `:235-237` + `:413-415` comments vs V2 plugins-guide transform-replay semantics (fetched 2026-09-23); `grep -n 'agents' tests/smoke.test.mjs` → **0 matches** (220 lines, 9 tests: registry sync / marker guards / agy fixture replays only); guards verified present at `:486`/`:496` (`editor.get("plan"/"build") !== undefined` → missing ids skip, no crash) and `:491`/`:517` (`!current.includes(PLAN_TAG/BUILD_TAG)` → suffix idempotent). |
| RK-003 | Low | **Lockfile mirror reachability/supply surface — adjudicated Low (was un-adjudicated by the refuter pass).** All 5 `resolved` URLs point at `registry.npmmirror.com`, but all 5 are platform-mismatched on the CI runner (`darwin-arm64`, `darwin-x64`, `linux-arm`, `linux-arm64`, `win32-x64`) → npm skips them on `ubuntu-latest` (x64); the one entry CI actually installs (`msgpackr-extract-linux-x64`, package-lock.json:577-587) has **no** `resolved` key → default registry. CI therefore fetches zero mirror bytes; latent exposure = mac/win local `npm ci` availability only, bounded by sha512 integrity (tamper fails closed) and the documented swap-registry fallback. First-push GH-runner liveness remains the recorded ship-release checkpoint. **Owner:** automation owner (already-assigned residual; severity set here, no fix freelanced). | 5/5 `resolved` hosts = npmmirror (grep); os/cpu filters per entry read at package-lock.json:525-599 with `integrity` sha512 on all 5; liveness probe 2026-09-23: `registry.npmmirror.com/msgpackr` → **200**, tgz HEAD → **200** (content-length 6875); npmjs.org → 200 (control). |
| RK-004 | Low | **CI verification lane executes an unpinned, latest-fetched toolchain.** `mise run typecheck` (ci.yml:18) runs `npx -y -p typescript tsc …` (mise.toml:9) → every fresh runner pulls the *newest* TypeScript with auto-confirm; a breaking or compromised release turns the lane red or yields CI code-execution in a read-only, no-secret context. Pre-existing task surfaced by REQ-001; absent from the spec's R-001..R-007 table; in tension with the checklist's "dependencies pinned" (zero-dep pattern deliberately chosen in REQ-NF-001). Failure mode is visible-red, never silent. **Owner:** engineering owner. | `mise.toml:9` (`npx -y -p typescript tsc …`); `ci.yml:18` (`run: mise run typecheck`); risk table `PROPOSED_CHANGES.md:56-64` (R-001..R-007) contains no unpinned-toolchain row; runners are fresh VMs → no npx cache. |
| RK-005 | Low | **E-003's single-revert attestation is stale for 3 of its 6 named units.** `TEST_MATRIX.md:17` claims each of `8548efe 7efa7f7 feb9014 ae97e42 0e556a1 1640510` is single-revertible; at HEAD, reverse-apply fails for `7efa7f7` (lockfile later modified by `32210cf`), `feb9014` (ci.yml later modified by `a83347b`), and `ae97e42` (README later modified by `98d8182`) — recorded pre-remediation and not refreshed by `dfd01b7`. Capability intact (revert newest-first, or delete the file; nothing in production, all unpushed) so REQ-NF-002's ≤15-minute rollback holds; the overstatement is in the evidence wording. **Owner:** engineering owner (matrix author lane). | `git show 7efa7f7 \| git apply --check -R` → `error: patch failed: package-lock.json:1`; `feb9014` → `error: patch failed: .github/workflows/ci.yml:1`; `ae97e42` → `error: patch failed: README.md:259`; same command CLEAN for `8548efe`, `0e556a1`, `1640510`, `45f3995`, `98d8182`, `a83347b`, `32210cf` (2026-09-23); E-003 text at TEST_MATRIX.md:17. |

## Risk Assessment Matrix

| ID | Risk Dimension | Impact | Likelihood | Mitigation | Residual |
|----|----------------|--------|------------|------------|----------|
| RK-001 | Evidence/provenance integrity (mirror origin misattributed) | Med | Med (already occurred) | Owner corrects the matrix line + mirrored refuter note (no freelance fix) | **Open — condition** |
| RK-002 | REQ-007 behavior-change enforcement (permissions semantics) | Med | Conditional (docs vs in-file comment conflict) | Sponsor approval; presence + idempotency guards verified; typecheck/test green (static) | **Open — condition** |
| RK-003 | Third-party mirror reachability/supply | Low | Low | sha512 integrity fail-closed; platform-skip ⇒ 0 CI fetches; swap-registry fallback documented; liveness probed 200 | Low — rides gate |
| RK-004 | Unpinned latest TypeScript in CI | Low | Low–Med over time | Visible-red only; read-token, no-secret env; zero-dep pattern is the stated trade | Low — rides gate |
| RK-005 | Revert-attestation drift vs HEAD | Low | Low | Actual rollback <15 min via newest-first revert; attestation refresh rides doc pass | Low — rides gate |
| RK-006 | Mutable major-tag actions (`checkout@v4`, `setup-node@v4`, `jdx/mise-action@v3`) | High-at-public-flip | Low | **Accepted risk, documented**: private repo, `permissions: contents: read`, no secrets; owner engineering owner; expiry = public flip → SHA pins + Dependabot (spec §4, proposal R-006) | Accepted (guardrail-compliant) |
| RK-007 | First CI run never executed (19 commits unpushed) | Med pre-push | n/a at gate | Commands replayed locally (T-001); all 3 `uses:` refs API-verified 200 today; `npm ci` green on npm 10.9.8 + 11.19.1 + 11.20.0 | Residual — ship-release checkpoint (first-green on push), owner engineering owner |

## Verdict Rationale

**conditional** — 2 Medium conditions with small, owner-side mitigations; 3 Lows recorded for gate-keeper disposition; no Critical/High.

- **Lens (a) — spec's own risk table:** R-001's pin-validity class materialized as CE-001 and is now covered for the shipped state: all three `uses:` refs independently resolved today — `actions/checkout@v4` → 200 (sha `11d5960a…`), `actions/setup-node@v4` → 200 (sha `49933ea5…`), `jdx/mise-action@v3` → 200 (sha `5228313e…`); the dead `jdx/action-mise` → 404 and is absent from `ci.yml`. Another ref cannot be *currently* wrong; recurrence vectors (future bad ref → first-push red; mutable-major retag) fall under R-006, accepted with owner + expiry per guardrails. R-002..R-007 re-checked: none newly materialized; R-007's presence/idempotency guards are proven safe — but R-007's *permission-effect* mitigation is what RK-002 disputes.
- **Lens (b) — supply chain:** `permissions: contents: read` (least privilege), zero secrets, triggers `push:main` + `pull_request` only — **no** `pull_request_target`/`workflow_run` untrusted-code execution; unprivileged PR code runs with read-only token in a secretless context. Major-tag mutability is the one accepted residual, properly documented (RK-006).
- **Lens (c) — blast radius:** bounded to 18 approved files; INV-007 untouched; comment-only hook edits; REQ-007 absent-agents path is fail-safe. Two exceptions: RK-002 (unproven enforcement) and RK-005 (stale revert wording).
- **Lens (d) — mirror reachability:** adjudicated RK-003 Low with platform-skip + integrity + liveness proof; GH-runner first push stays the checkpoint (RK-007).
- **Lens (e) — history distortion:** CHANGELOG is **clean**: restored `## [v0.7.0] — 2026-09-19` byte-matches `git show e496c25:CHANGELOG.md`; `0e556a1` = +1/−5 structural only (zero bullet lines touched → v0.6.1 merge verbatim); `45f3995` = 4 bullets moved verbatim + 1 preserved, position below `v0.3.1`, heading counts 1/1/1, and no section anywhere has a doubled `### Added` (per-section count max = 1); merged heading date 09-15 is one of the two originals from `19532cd` — nothing invented. The one prose-history falsehood found lives in gate evidence, not the changelog → RK-001.
- **pass weighed and rejected:** RK-001 falsifies a verification claim inside the very chain REQ-NF-003 certifies, and RK-002 leaves the sole HARD-exempted behavior change without runtime evidence while the repo's own comments contradict the mechanism it relies on — risk review cannot pass unproven enforcement plus falsified provenance.
- **fail weighed and rejected:** no Critical/High; every substantive dimension re-proven green independently (pins resolve, CHANGELOG byte-provenance, zero-dep, blast-radius bounds, rollback capability, least-privilege workflow); all Lows have owners and available mitigations.
- **Conditions to clear (gate keeper maps to COND-00x):** (1) RK-001 — correct the provenance line at `TEST_MATRIX.md:25` to attribute `registry.npmmirror.com` to `32210cf`, with the mirrored note at `refuter.md:71` re-recorded by its author; (2) RK-002 — engineering owner adjudicates the permissions contradiction: either a runtime observation of `planPermissions()` persistence against V2 transform-replay semantics, or amend the `:233-237`/`:413-415` comments to their proven scope plus a recorded accepted residual. Lows RK-003..005 ride the gate (RK-005 wording may fold into the ship-release doc pass).
- No freelance fixes performed: findings only, each with severity + location + owner; remediation belongs to the owners. Exactly one file written; git read-only throughout; zero PII/secrets/tokens in this artifact.

---

## Re-verification (post-remediation, 2026-09-23)

Condition-clearance pass on this reviewer's **own five findings** after owner remediation — not a full re-review, no other reviewer's role performed. Every disposition below was re-proven with fresh evidence today; git read-only (log/show/diff/status/apply-check only); this file remains the only write; zero PII/secrets. The checklist, findings table, and rationale above stand as the pre-remediation record; this section supersedes them.

**Header verdict: conditional → pass.** Both blocking Mediums are dispositioned — RK-001 cleared; RK-002's contradiction cleared with its runtime remainder carried on a bar-compliant waiver. RK-003 and RK-005 cleared by owner fixes; RK-004 accepted at its unchanged Low severity (backlog, non-blocking). No Critical/High finding existed at any point.

| ID | Status | Decisive evidence (re-run 2026-09-23) |
|----|--------|----------------------------------------|
| RK-001 | **CLEARED** | `TEST_MATRIX.md:25` rewritten (commit `449f386`, file re-read in full today): the false claim is now *named as false* — "the earlier 'pre-existing since `7efa7f7`, all resolved URLs' claim in this line was false): `7efa7f7` had **zero** `resolved` fields; the 5 … URLs were introduced by `32210cf` … stripped — `grep -c npmmirror` → 0 … Residual closed; refuter note aligned". `refuter.md:78-90` carries a dated **Addendum (2026-09-23 — mirror residual provenance correction)** that independently re-executes `git show 7efa7f7:package-lock.json \| grep -c '"resolved"'` → **0**, attributes `32210cf` (proven: `git show 32210cf:package-lock.json \| grep -c npmmirror` → 5, all in added lines), declares "Mirror residual status: **CLEARED**", and states "residual 3 above is superseded by this addendum (history preserved, not deleted)" — so `refuter.md:71` is historically-scoped, not a live claim. Sweep: `grep -rn "pre-existing since" docs/specs/` → exactly 5 hits, every one non-live: the correction text itself (`TEST_MATRIX:25`), the negating addendum (`refuter:81`, `:85`), the superseded original carrying the pointer (`refuter:71`), and this findings table (`risk.md:24`, retained by design). **Zero live false provenance claims remain anywhere in the evidence chain.** |
| RK-002 | **CLEARED (contradiction) + CARRIED-AS-WAIVER (runtime residual)** | `git show fdda81a` (full patch read): comment block at `agents.ts:478-489` only — 9 insertions/4 deletions, every changed line a `//` comment, `await ctx.agent.transform(` untouched → **zero logic change** as attested (prior typecheck/test attestations remain valid — no code delta). The transform comment now states description overrides "stay commented by REQ-007 design — built-in descriptions untouched" (matches the commented assignments at `:493`/`:503`; the old comment's claim of *active* Frame-lane/Ship-lane descriptions is gone) and "permission persistence through this transform is host-reconciled (see the setup()/serializer notes above): effective enforcement must be confirmed in a live host session — gate-tracked residual, engineering owner, expiry at first use" — it now defers to, instead of contradicting, the serializer/setup notes (`:234-237`, `:414-415`, re-read today, scoped to file-emission/setup facts). Residual recorded in `PROPOSED_CHANGES.md:83` (commit `f14cc4c`, working tree verified clean at HEAD `449f386`): "live-host permission confirmation … carried as C3 waiver records with expiry at ship-release v0.12.0", enumerated with plan step 9 (RD-004+risk-M2). Runtime fact honestly disclosed and re-confirmed: `grep -c agents tests/smoke.test.mjs` → **0** (suite still cannot observe host reconciliation). This is the *second* alternative my pass-1 condition (2) expressly offered — reconcile comments to proven scope **plus a recorded accepted residual** — the goalposts did not move; bar assessed below. |
| RK-003 | **CLEARED** | `git show 2de0839` → 5 deletions only, exactly the 5 mirror `resolved` lines (entries themselves intact — the 5 `@msgpackr-extract/*` platform keys remain: 10 grep hits across package keys + `optionalDependencies` refs). Current state: `grep -c registry.npmmirror.com package-lock.json` → **0**; `grep -c '"resolved"'` → **0** (registry-default shape, matching `7efa7f7`). **Independent runs by this reviewer today:** `npm ci --dry-run` → **exit 0** (npm 10.9.8); `npx -y npm@11 ci --dry-run` → **exit 0** (npm 11.20.0). The finding's subject — mirror reachability — is eliminated: zero mirror URLs exist to reach. Surface removed, not merely adjudicated Low (also fulfills `refuter.md:86`'s "commit stays with the owner" — landed as `2de0839`). |
| RK-004 | **BACKLOG-ACCEPTED (non-blocking)** | Not fixed by design — `mise.toml:9` unchanged and absent from the wave-2 scope (plan step 9 enumerates RD/QA/RL/AUT/risk items; no TS-pin item). **My call, reasoning:** (1) severity ladder — Low → backlog; nothing in remediation raised it; (2) failure mode is visible-red only (CI typecheck fails loudly — never silent, never data loss); (3) blast bounded — the workflow runs `permissions: contents: read` with zero secrets, so even a compromised latest-TypeScript executes on an ephemeral runner holding only a read-only, repo-scoped token; (4) the obvious fix (pin TS as a dependency) collides with the HARD zero-dep invariant of REQ-NF-001 — the pin shape (a zero-dep-compatible npx version-spec exists) is an owner design call, recorded here, not freelanced; (5) not a spec-coverage regression — the item was never in R-001..R-007. Stands at Low, owner engineering owner, **does not block the gate.** |
| RK-005 | **CLEARED** | Matrix E-003 rewritten (commit `449f386`, row re-read): "Per-REQ units revert in reverse commit order (later same-file fixes first) — rollback <15 min … CI disable = revert `a83347b` then `feb9014` (merge-tree verified clean — a lone `feb9014` revert conflicts post-`a83347b`) or delete the workflow file … (Wave-2 correction: original 'single-revertible / revert `feb9014`' attestation **refuted** by QA-002/AUT-002/risk-5.)" — the stale claim is replaced by its own refutation, and the disclosed conflict is exactly my pass-1 proof (`git show feb9014 \| git apply --check -R` → `error: patch failed: .github/workflows/ci.yml:1`). Proposal Rollback Plan carries the same two-SHA path (commit `f14cc4c`, working tree verified clean at HEAD `449f386`: "revert `a83347b` then `feb9014` in that order … line 16 was modified by `a83347b`"). Sequence soundness re-proven from my own data: `a83347b` reverse-apply **CLEAN** at HEAD, and `git log --oneline -- .github/workflows/ci.yml` → exactly `[a83347b, feb9014]` — so after step 1, `ci.yml` ≡ feb9014's post-image and step 2 is the exact inverse of its own diff → clean by construction. Capability (≤15 min) was never in doubt; the wording was the finding, corrected in both artifacts. |

### Waiver-bar assessment (RK-002 live-host residual)

The bar for a condition of this kind: an accepted-risk record with **owner + justification + expiry**, compensating controls, and explicit residual risk. Assessed against the artifacts as written — **MET**:

- **Owner:** engineering owner (`agents.ts:489`, plan step 9). ✓
- **Justification:** a live host session is structurally unavailable during a gate run, and the static evidence chain (diff + typecheck + 9-test suite, `grep -c agents` → 0) *cannot* observe host reconciliation. Demanding runtime proof from the stage would demand evidence the stage cannot produce; recording the gap with a checkpoint is the honest move. ✓
- **Expiry:** recorded twice — comment "first use"; proposal "ship-release v0.12.0". Both concrete; **unify at gate, earlier governs** (non-blocking observation for the gate keeper, not a new finding). ✓
- **Compensating controls:** presence guards (`:491`/`:501` — missing ids skip, no crash), suffix idempotency tag checks (`:496` + build counterpart), description overrides stay commented (REQ-007 design), zero logic change (full `fdda81a` patch), prior typecheck/test attestations remain valid pre-change, per-unit revert rollback intact. ✓
- **Residual risk, explicit (no sugarcoating):** if permission persistence turns out not to bind, the intended docs-only restriction on `plan` does not enforce and the agent runs at host defaults — an *internal* blast radius (private repo, single maintainer, built-in agent, no external trust boundary), disclosed, owner-named, expiry-bound. Accepted risk, not an unmitigated one.

**Formal waiver sign-off** (domain owners + orchestrator block, per guardrails) is gate-record work and rides `GATE_REPORT.md` — the gate keeper's lane. This verdict assesses the *bar*, not the signatures.

### Why pass now (supersedes pass-1 rationale)

- Pass-1 rejected `pass` on two grounds; both are gone: (i) falsified provenance → corrected at source, mirrored note addendumed, sweep shows only historically-scoped survivors; (ii) unproven-and-contradicted REQ-007 → contradiction reconciled comment-side with zero logic change, and the unproven remainder moved onto the accepted-risk record via the alternative my own condition (2) offered.
- `fail` was never on the table (no Critical/High) and every substantive dimension re-proven green again today: both npm majors exit 0, mirror grep 0, `agents.ts` diff comment-only, revert-sequence disclosure consistent with the proven apply-check results, blast-radius and zero-dep invariants untouched (HEAD `449f386`, tree clean but for this untracked gate dir, 24 commits unpushed).
- All three Lows dispositioned: RK-003/RK-005 fixed and cleared, RK-004 accepted at severity (Low → backlog per the severity ladder).
- **Carry-overs for the gate report (gate keeper, non-blocking):** (1) RK-002 waiver sign-off + expiry unification (first use vs v0.12.0); (2) pre-existing checkpoints unchanged — first-CI-green at push (refuter residual 1, owner engineering owner) and R-006 public-flip → SHA pins.
- Dispositions only — no freelance fixes; remediation stayed with owners (orchestrator, engineering owner). Git read-only throughout; exactly one file written: this verdict.
