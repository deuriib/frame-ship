# Reliability Review: SPEC-repo-hygiene

**Reviewer:** review-reliability
**Date:** 2026-09-23
**Verdict:** pass
*Initial review: conditional → cleared post-remediation; see `## Re-verification (post-remediation, 2026-09-23)` at the end.*

## Checklist

- [x] Error paths handled explicitly — probed live: empty stdin → safety-gate `{"decision":"ask"}` exit 0; malformed JSON → safety-gate `ask`, context-inject `{}`, format-note `{}` (all exit 0). Fail-safe, never crash, never inject on bad input. (Residual: not suite-locked → RL-004.)
- [~] No swallowed exceptions — every `catch` in the hooks degrades to a visible fallback (`ask` decision / fallback bootstrap text / `{}`), never a silent success that hides a failure; `bump-version.mjs` logs to stderr on per-file error. No empty catch found. Pass on behavior; note `readFileIfExists` swallows per-candidate read errors but the candidate loop ends in an explicit user-facing fallback message.
- [x] Input validation at boundaries — hooks narrow stdin payload with `typeof`/`Array.isArray`/`String()` coercion before use; unknown shapes degrade to safe defaults.
- [x] Deterministic behavior (no hidden state) — no sleeps, no wall-clock reads in tests; `spawnSync` bounded (`timeout: 15000`); suite green under **two runtimes**: node v26.9.0 (9/9, exit 0) and CI's node v22.23.2/npm 10.9.8 (9/9, exit 0).
- [~] Edge cases tested (empty, null, max, boundary) — empty/malformed payloads behave fail-safe under my probes, but the committed suite carries no empty/malformed fixture (REQ-002(c) enumerated only the 5 happy-path replays) → RL-004.
- [x] Idempotency where required — `hasMarker` guard precedes every `event.system.push` in both hooks (`plugins/opencode/skills.ts:176,186` vs pushes at `177,181,187`); structural test extracts both hook bodies and asserts guard-before-first-push; both hook names registered exactly as the extractor expects.
- [ ] Timeouts on external calls — **not satisfied**: antigravity hooks do blocking stdin/file reads with no deadline while `plugins/AGENTS.md:23` claims a 2000ms `withTimeout` deadline (→ RL-001); CI jobs set no `timeout-minutes` (→ RL-005).

## Failure Modes Analyzed

| ID | Failure Mode | Expected Behavior | Handled? |
|----|--------------|-------------------|----------|
| FM-001 | Node/npm version variance (local vs CI runner) | tests + entry point behave identically on node 22.23.2/npm 10.9.8 and node 26.9.0/npm 11.19.1 | yes — 9/9 exit 0 on both; bare `node --test tests/` exit 1 on node 22.23.2 reproduced, quoted-glob form correct |
| FM-002 | Fresh-runner CI sequence fails (bad action ref, missing mise, command error) | both jobs succeed on ubuntu-latest from a cold checkout | yes — `jdx/mise-action` + `refs/tags/v3` API-verified (200), `actions/checkout`/`setup-node` `refs/tags/v4` exist; full sequence replayed locally: `npm ci` (npmjs) exit 0 → `mise run typecheck` exit 0 → `bump-version.mjs --check` exit 0 |
| FM-003 | `npm ci` diverges across npm majors (CE-002 class) | dry-run/install exit 0 on npm 9, 10, 11 | yes — `npm ci --dry-run` exit 0 under npm 9, 10, 11.19.1 (default), 11.20.0; full `npm ci` exit 0 under npmjs config (2×) — remediation independently reproduced across **three** npm majors (matrix recorded two) |
| FM-004 | Lockfile `resolved` host unreachable on runner (npmmirror URLs) | CI fetches only the configured registry | yes with residual — all 5 npmmirror entries are platform-mismatched on ubuntu-x64 (darwin\*/win32/linux-arm\* → never fetched), **and** npm 9/10/11 rewrite foreign hosts to the configured registry (forced full install: npmmirror hits 0/0/0) → runner reachability class = registry.npmjs.org; non-default-config tail → RL-002 |
| FM-005 | Malformed/empty hook payload | fail-safe, exit 0, no injection, no secret echo | yes (probed) — but untested in suite → RL-004 |
| FM-006 | Hook stdin never closes / mount stall mid-read | bounded wait then safe default (per `plugins/AGENTS.md:23` claim) | **no** — blocking `readFileSync(0)`/file reads with no timer in any of the 3 antigravity hooks → RL-001 |
| FM-007 | Duplicate system injection across turns | marker guard makes each hook body fire once | yes — structural test + guard/push index assertion green |
| FM-008 | Version drift across the 7 lockstep files (incl. CI `--check` gate) | drift → exit 1; synced → exit 0; bump must not break `npm ci` | yes — `--check` exit 0 on all 7; exit-1 path present in code; simulated `package.json` 0.12.0 vs lockfile 0.11.0 → `npm ci --dry-run` exit 0 (ship-release bump safe) |
| FM-009 | CI job hangs on network stall | job terminates within a bounded window | no — no `timeout-minutes`; GitHub default 360 min → RL-005 |

## Findings

| ID | Severity | Finding | Proof |
|----|----------|---------|-------|
| RL-001 | Medium | **Bounded-IO claim is false for the antigravity hooks.** `plugins/AGENTS.md:23` states file reading "in hooks and plugin setup uses a 2000ms deadline (`withTimeout`) to prevent hanging on disk or mount stalls"; all three hooks do blocking reads with no deadline — stdin `readFileSync(0)` at `safety-gate.ts:47`, `context-inject.ts:76`, `format-note.ts:23`, plus unbounded `readFileSync` of `SKILL.md` (`context-inject.ts:83`). **Location:** `plugins/AGENTS.md:23` + `plugins/antigravity/hooks/*.ts`. **Owner:** engineering owner. Conditional impact: harness stalls mid-pipe or a mount stall (the exact scenario the claim promises to prevent) blocks a PreInvocation/PreToolUse hook indefinitely → session/tool-call hang, no fail-closed, no timeout. In a file already inside REQ-004's sweep blast radius, shipping a knowingly-false safety claim contradicts this spec's evidence-over-claims thesis. | `grep -rn withTimeout plugins/` → only `shared.ts:70,111` (opencode lane); zero hits in `plugins/antigravity/hooks/`; line 23 claim read verbatim; probes show payloads handled fail-safe but nothing bounds read duration (`readFileSync(0)` blocks until EOF by contract) |
| RL-002 | Low | **Lockfile carries 5 third-party-mirror `resolved` URLs leaked from the fixer's registry config.** `registry.npmmirror.com` appears in exactly the 5 CE-002-added optional entries; the other 285/290 package entries carry no `resolved`/`integrity` at all (unusual shape, but version-pinned). Fetch behavior verified immune across all three npm majors; residual risk only under non-default config (`replace-registry-host=never` → literal mirror fetch — mirror fragility demonstrated same day). **Location:** `package-lock.json:527,540,553,566,590` (commit `32210cf`). **Owner:** engineering owner. | host census: `resolved` occurrences = 5, all `registry.npmmirror.com`; provenance: user-level `~/.npmrc` sets `registry=https://registry.npmmirror.com`; swap probe: `npm@9/10/11 ci --force --registry=https://registry.npmjs.org` → exit 0/0/0, `grep -ci npmmirror` = 0/0/0 (mirror host rewritten to configured registry); CI platform: entries are darwin-arm64/darwin-x64/linux-arm/linux-arm64/win32-x64 — none fetchable on ubuntu-x64; fragility class: full `npm ci` under mirror config → `ECONNRESET … registry.npmmirror.com/zod` (retry via npmjs → exit 0) |
| RL-003 | Low | **CI typecheck toolchain is unpinned — determinism degrades over time.** `npx -y -p typescript` resolves the `latest` dist-tag on every run; a future TS major shipping new diagnostics turns CI red with zero repository change. Spec REQ-NF-001 froze the zero-dep pattern, but spec §4's accepted-residuals record covers only action major-tags, not TS drift (pinning a version stays zero-dep — reported as risk only, no fix freelanced). **Location:** `mise.toml:9` (used by `npm run typecheck`, `mise run typecheck`, and the CI typecheck job). **Owner:** engineering owner. | `npx -y -p typescript tsc --version` → **Version 7.0.2** pulled today; typecheck green today (exit 0 after `npm ci`) — proof the unpinned fetch is live and already on a new major |
| RL-004 | Low | **Hook error paths (empty/malformed stdin) verified working but not locked by the suite.** A regression removing a fail-safe `catch` (e.g., safety-gate emitting unparseable stdout or non-zero exit) would ship undetected — REQ-002(c) enumerated only the 5 happy-path replays, and no empty/malformed fixture exists. **Location:** `tests/smoke.test.mjs` (coverage gap) / `plugins/antigravity/hooks/fixtures/`. **Owner:** engineering owner. | probes: `echo "" \| node …/safety-gate.ts` → `{"decision":"ask",…}` exit 0; `echo "not-json{"` → `ask` exit 0; context-inject empty+garbage → `{}` exit 0; format-note garbage → `{}` exit 0; `git ls-files …/fixtures/` → 6 fixtures, none empty/malformed; suite = the 5 spec-mandated replays |
| RL-005 | Low | **No `timeout-minutes` on either CI job.** A network-stall hang inside `npm ci`/`npx` (stall class demonstrated live today, RL-002 proof) burns GitHub's default 360-minute runner window per occurrence. **Location:** `.github/workflows/ci.yml` jobs (`typecheck` L12-19, `test` L21-29). **Owner:** automation/ops owner (ops mechanics: engineering owner). | `grep -c timeout-minutes .github/workflows/ci.yml` → 0; GitHub default job timeout 360 min; same-day `ECONNRESET` during `npm ci` proves the stall class reachable in this supply path |

## Verdict Rationale

Every requirement claim in my lens was independently re-executed; nothing inherited from `refuter.md` — its pass post-remediation was treated as context only, and its two conditions were re-proved from scratch: **CE-001** (action ref) → GitHub API `refs/tags/v3` exists on `jdx/mise-action` (200), old `jdx/action-mise` dead, pin `a83347b` correct; **CE-002** (npm majors) → `npm ci --dry-run` exit 0 under npm **9, 10, 11.19.1, 11.20.0** — I extended the matrix's two-major verification to all three majors myself. Test determinism holds across node 22.23.2 and 26.9.0 (9/9 both, no sleeps, bounded spawns), the CI sequence replays green end-to-end from a cold `npm ci`, version-lockstep `--check` is green on all 7 targets with a working exit-1 drift path and a bump that provably won't break `npm ci`, and hook payload handling is fail-safe on every malformed input I threw at it.

`conditional`, not `pass`: my checklist's "Timeouts on external calls" item cannot be checked — RL-001 shows the antigravity hooks have no bound while a file inside this spec's own doc-sweep claims they do, and a false safety claim in a swept file is exactly what this spec exists to eliminate. `fail` was weighed and rejected: no requirement is falsified — RL-001 is pre-existing scope-adjacent behavior with an owner and a bounded mitigation, and RL-002..005 are Low/hygiene with proven default-path immunity (npmmirror hosts are rewritten on all three npm majors; CI never fetches them).

**Condition to clear (gate keeper maps to COND-00x):**
- **COND-RL-01 (RL-001, Medium):** reconcile `plugins/AGENTS.md:23` with hook reality — either the reads get bounded or the claim gets corrected — verified by reviewer re-run. Owner: engineering owner.

**Recorded, non-blocking (backlog):** RL-002, RL-003, RL-004, RL-005 — each proof-backed with severity/location/owner above; none invalidates a REQ, none is freelanced.

*(Superseded by the post-remediation re-verification below: COND-RL-01 cleared by `4526b63`, RL-002 cleared by `2de0839`, RL-003/004/005 accepted at Low = backlog.)*

---

## Re-verification (post-remediation, 2026-09-23)

Condition-clearance pass on MY OWN findings only — every disposition below re-proved from fresh commands run today (diffs read via `git show`, nothing taken on trust; git read-only throughout). Original findings table above kept intact as the record of the initial review.

| ID | Original severity | Status | Decisive evidence (re-run today) |
|----|-------------------|--------|----------------------------------|
| RL-001 | Medium (COND-RL-01, blocking) | **CLEARED** | `git show 4526b63 -- plugins/AGENTS.md` re-read: bullet now reads "File reads in plugin setup (the `shared.ts` helpers) use a 2000ms deadline (`withTimeout`) … hook stdin reads are unbounded by design — the harness owns the pipe and closes it after the payload". Doc now matches code on both halves: positive claim true (`grep shared.ts` → `READ_TIMEOUT_MS = 2000` at :68, `withTimeout` defined :70, applied :111); negative claim true (all three hooks still `readFileSync(0)` with no timer — `safety-gate.ts:47`, `context-inject.ts:75`, `format-note.ts:23`), and my original fail-safe probes re-run → malformed payload → `{"decision":"ask"}` / `{}` / exit 0. The original finding was the doc-vs-code **mismatch** (a false safety claim); the claim is corrected with rationale, which is exactly the clearing option my condition stated. Residual: the unbounded pipe read itself remains — now a *documented* owner-accepted design (justification in-line: harness owns the pipe; owner: engineering owner), not a hidden hazard. |
| RL-002 | Low | **CLEARED** | `git show 2de0839` — exactly the 5 `resolved` lines removed, `integrity` + `optional`/`cpu`/`os` untouched, `package.json` not in the diff. Re-runs: `grep -c npmmirror package-lock.json` → **0**; `"resolved"` occurrences → **0** (matches `7efa7f7` zero-resolved baseline, restoring registry-default resolution); `JSON.parse` valid on working tree AND on the committed blob (`2de0839:package-lock.json`, 291 packages incl. root); **`mise exec node@22 -- npm ci --dry-run` (npm 10.9.8) → exit 0**; **`npx -y npm@11.20.0 ci --dry-run` → exit 0** — both majors run by me, not inherited; `npm test` still 9/9 pass, 0 fail; provenance correction recorded in matrix by `449f386` (lockfile blame: zero-resolved at `7efa7f7`, mirror URLs introduced by `32210cf`, stripped by `2de0839`). |
| RL-003 | Low | **BACKLOG-ACCEPTED** | Still present as recorded: `mise.toml:9` = `npx -y -p typescript` (unversioned). Green today (TS 7.0.2 earlier this session, typecheck exit 0). Non-blocking by my call: guardrail severity Low = hygiene = backlog, and pinning would edit the typecheck contract REQ-NF-001 explicitly froze (`npx -y -p typescript`) — that is a spec-amendment decision for the owner, not a gate item. Owner: engineering owner. |
| RL-004 | Low | **BACKLOG-ACCEPTED** | Still present as recorded: `fixtures/` holds 6 payloads, none empty/malformed; no new test file (`tests/*.test.mjs` → smoke only). Error-path behavior re-probed today and still fail-safe (malformed → `ask`/`{}`, exit 0). Non-blocking: the *behavior* is verified by review probes; only the regression-lock is missing — hygiene at Low. Owner: engineering owner. |
| RL-005 | Low | **BACKLOG-ACCEPTED** | Still present as recorded: `grep timeout-minutes .github/workflows/ci.yml` → 0 matches. Non-blocking: GitHub's platform default (360 min) already bounds job runtime — no unbounded resource burn, commands are short local npm/CI ops — classic Low hygiene, owner-assigned. Owner: automation/ops owner (ops mechanics: engineering owner). |

**Re-verification verdict rationale → `pass`.** The single blocking condition COND-RL-01 is cleared with fresh proof (claim corrected, both halves of the new statement verified against `shared.ts` and the three hooks). RL-002 cleared across both required npm majors with commands I ran myself today. RL-003/004/005 remain exactly at the Low severity I originally assigned — guardrails: Low = hygiene = backlog — each carrying severity + location + owner in the original findings table, none invalidating a REQ, none escalated by evidence. Nothing genuine remains unmet, so the header verdict moves from `conditional` to `pass`.
